<main class="main">
  <div class="inner">
    <% if user_signed_in? %>
      <div class="greeting">
        <%= link_to "こんにちは、", class: :greeting__link %>
        <%= link_to current_user.name, root_path, class: :greeting__link%>
        <%= link_to "さん", class: :greeting__link%>
      </div>
    <% end %>
    <div class="card__wrapper">
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
    </div>
  </div>
</main>
